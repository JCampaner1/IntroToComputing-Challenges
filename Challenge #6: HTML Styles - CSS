<!DOCTYPE html>
<html lang="en">
<head>
    <title>My Simple Blog</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="This is a simple website showcasing basic web design with HTML.">
    <meta name="keywords" content="simple, website, HTML, web design, basic">
    <meta name="author" content="[Your Name]">
    
    <!-- Favicon Implementation -->
    <link rel="icon" type="image/x-icon" href="favicon.ico">
    
    <!-- CSS Style Section -->
    <style>
        /* General Styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }

        /* Header Styles */
        header {
            background-color: #333;
            color: white;
            padding: 20px;
            text-align: center;
        }

        /* Navigation Styles */
        nav ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
            background-color: #333;
            overflow: hidden;
        }

        nav ul li {
            float: left;
        }

        nav ul li a {
            display: block;
            color: white;
            text-align: center;
            padding: 14px 20px;
            text-decoration: none;
        }

        nav ul li a:hover {
            background-color: #575757;
        }

        /* Main Content Styles */
        section {
            padding: 20px;
        }

        h2 {
            color: #333;
        }

        p {
            line-height: 1.6;
        }

        /* Footer Styles */
        footer {
            background-color: #333;
            color: white;
            text-align: center;
            padding: 10px;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
    </style>
</head>
<body>
    <!-- Header section -->
    <header>
        <h1>Welcome to My Simple Blog</h1>
    </header>

    <!-- Navigation menu -->
    <nav>
        <ul>
            <li><a href="#home">Home</a></li>
            <li><a href="#about">About</a></li>
            <li><a href="#contact">Contact</a></li>
        </ul>
    </nav>

    <!-- Main content area -->
    <section id="home">
        <h2>Home Section</h2>
        <p>This is the home section of the blog.</p>
    </section>

    <section id="about">
        <h2>About Section</h2>
        <p>This is the about section. Share something about yourself or the site here.</p>
    </section>

    <section id="contact">
        <h2>Contact Section</h2>
        <p>This is the contact section. You can add contact information or a form here.</p>
    </section>

    <!-- Footer section -->
    <footer>
        <p>&copy; 2024 My Simple Blog</p>
    </footer>
</body>
</html>
